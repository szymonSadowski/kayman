import type { Config } from '@kayman/shared'

// Implementation: Story 2.3
export async function startCommand(
  _project: string | undefined,
  _config: Config,
): Promise<void> {
  throw new Error('Not yet implemented — Story 2.3')
}
