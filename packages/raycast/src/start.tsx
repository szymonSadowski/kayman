import { List } from '@raycast/api'

// Implementation: Story 4.3
export default function Start() {
  return <List><List.EmptyView title="Start Recording — not yet implemented (Story 4.3)" /></List>
}
